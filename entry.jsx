import React from 'react';
import ReactDOM from 'react-dom';
import Matrix from './components/matrix';

document.addEventListener('DOMContentLoaded', () => {
  const root = document.getElementById('root');
  ReactDOM.render(<Matrix />,root);
});
